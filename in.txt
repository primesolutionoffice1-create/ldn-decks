Da. Dă-i la developer să facă Faza 1 în cod, nu mai mult.

Please implement Phase 1 only.

1. Add noindex, follow to all /near-you/* pages
    Preferred: meta robots in the page/template:
    <meta name="robots" content="noindex,follow">

Do not delete these pages yet.

2. Remove from sitemap:

* all /near-you/* URLs
* /llms.txt
* /llms-full.txt
* legacy junk URLs if present

3. Fix future blog dates
    Find all BlogPosting schema where datePublished is after today and replace with the real publish date.
4. Fix broken showcase page:

* /showcase/rooftop-deck-washington-dc

If real project exists: rebuild it later.
For now, set noindex, follow or remove from sitemap until rebuilt.

5. Fix trailing slash duplicates
    Make sure only one version resolves as 200. Preferred canonical version: no trailing slash.
    Redirect slash version → non-slash version with 301.

Check:

* /composite-decks/
* /deck-repair/
* /trex-decks/
* /deck-remodeling/
* /services/porches/front-porch/

6. Fix legacy migration URLs
    301 redirect to the closest modern equivalent.
    If no equivalent exists, return 410.

Examples:

* /home-2/ → 410
* /deck-construction-in-reston/ → /deck-builder-reston-va
* /deck-balcony-resurfacing-in-sterling/ → relevant Sterling/showcase page or 410 if no real page

7. After deploy:

* verify noindex on /near-you/loudoun-county/aldie
* verify sitemap no longer contains /near-you/, llms.txt, or llms-full.txt
* verify trailing slash redirects return 301
* submit updated sitemap once in GSC

Do NOT:

* delete city pages
* noindex city pages
* noindex service pages
* rebuild all showcase pages in bulk
* publish new city pages
* request indexing for dozens of URLs

This is a controlled crawl-trust cleanup only.